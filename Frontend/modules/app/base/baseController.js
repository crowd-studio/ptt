'use strict';var $ = require('jquery'),	_ = require('lodash');module.exports = /*@ngInject*/function baseController($scope, $rootScope) {	//uploadrequired parsley	// window.Parsley.addValidator('uploadrequired', {	// 	requirementType: 'string',	// 	validateString: function (value, requirement, parsleyInstance) {	// 		return angular.element(parsleyInstance.$element).scope().loaded;	// 	},	// 		messages: {	// 		en: 'This value is required'	// 	}	// });	//Session notification	// notification.setNotifications(conf.notifications);	/*for (var type in conf.notifications) {		if (conf.notifications.hasOwnProperty(type)) {			for (var noti in conf.notifications[type]) {				if (conf.notifications[type].hasOwnProperty(noti)) {					notification.setNotification({						class: type,						message: conf.notifications[type][noti]					});				}			}		}	}*/};