
{% if params %}
	{% import 'PttBundle:Default:utilities.html.twig' as utilities %}

	{% set params = {
		type: "text",
		name: false,
		id: false,
		value: false,
		placeholder: false,
		
		label: false,
		placeholder: false,
		errors: true,

		outterClass: false,
		elementClass: false,
		size: 'medium',

	} | merge (params) %}

	{% set validations = {
		mapped: true,
		required: false,
	} | merge (params.validations | default({}) )  %}

	{{dump(params)}}

	<div class="form-group col-xs-12 {{utilities.getSize(params.size)}}{{ params.outterClass ? ' ' ~ params.outterClass : '' }}">
		{%- if params.label and params.label != "" -%}
			<label{{ utilities.attr('for', params.id) }}>{{ params.label }}{{ validations.required ? ' *' : '' }}</label>
		{%- endif -%}
		<div class="errors" {{- ( params.id is defined ? ' id="error-' ~ params.id ~ '"' : '' ) | raw -}}></div>
		<select class="form-control{{ params.elementClass ? ' ' ~ params.elementClass : '' }}"{{ utilities.attrGroup([ ['name', params.name], ['id', params.id], ['data-parsley-errors-container', params.errors ? '#error-' ~ params.id : false] ]) }}{{ validations.required ? ' required' : '' }}>
			{%- if params.placeholder -%}
				<option value{{ params.value == false or params.value == "" ? ' selected' : '' }}>{{ params.placeholder | trans }}</option>
				<option disabled>---</option>
			{%- endif -%}
			{% for itemValue, itemLabel in params.list %}
				<option value="{{ itemValue }}"{{ params.value == itemValue ? ' selected' : '' }}>{{ itemLabel }}</option>
			{% endfor %}
		</select>
	</div>
{% endif %}
