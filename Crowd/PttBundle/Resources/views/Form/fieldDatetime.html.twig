{% set params = {
	size: 'medium',
	format: 'DD/MM/YYYY HH:mm',
	attr: [
		'datetime'
	],
	addon: 'glyphicon glyphicon-calendar'
} | merge (params | default({})) %}

{% set params = params | merge({
	type : 'text'
}) %}

{% include 'PttBundle:Form:fieldInput.html.twig' %}