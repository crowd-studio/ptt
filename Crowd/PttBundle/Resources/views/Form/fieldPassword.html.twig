{% include 'PttBundle:Form:fieldInput.html.twig' %}

{% set validations = params.validations | merge ({
	required: params.validations.required is defined ? params.validations.required : false,
	minlength: params.validations.minlength is defined ? params.validations.minlength : false,
	maxlength: params.validations.maxlength is defined ? params.validations.maxlength : false,
	equal: params.name})
 %}

{% set params = {
	type: "password",
	name: params.name ~ '_repeated',
	
	label: params.label is defined ? 'Repeat ' ~ params.label : false,
	size: params.size is defined ? params.size : 'long',
	validations: validations
} %}

{% include 'PttBundle:Form:fieldInput.html.twig' %}